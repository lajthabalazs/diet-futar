Kedves {{ user.familyName }} {{ user.givenName }}!

Köszönjük, hogy megtisztelt bizalmával, és {{userOrder.orderDate.strftime('%Y-%m-%d %H:%M')}} -kor a következő rendelést adta föl:
		{% for orderItem in userOrder.items %}
				{% if orderItem.orderedItem == None %}
				{{ orderItem.orderedComposit.day }}
				{% for component in orderItem.orderedComposit.components%}{{component.menuItem.dish.title}}{% for subItem in component.menuItem.components%}, {{subItem.dish.title}}{% endfor %}{% endfor %}
				{{orderItem.itemCount}} adag
				{{orderItem.orderedComposit.price}} Ft/adag
				{{orderItem.price}} Ft
				{% else %}
				{{ orderItem.orderedItem.day }}
				{{orderItem.orderedItem.dish.title}}
				{{orderItem.itemCount}} adag
				{{orderItem.orderedItem.price}} Ft/adag
				{{orderItem.price}} Ft
				{% endif %}
		{% endfor %}
		
		Összesen{{userOrder.price}} Ft
Amennyiben rendelését módosítani szeretné, látogasson el a szállítás napját megelőző nap 12:00 előtt oldalunkra a http://www.diet-futar.hu címre. Lemondáshoz rendeljen a korábbi rendeléseinek megfelelő negatív mennyiséget.

Üdvözlettel: Diet-futár